<?php
/** @var \Magento\Framework\View\Element\Template $block */
/**
 * Ad Banner — placeholder for future product advertisement module.
 *
 * To use: replace the placeholder card(s) below with real ad content or
 * swap this template out via layout XML once a dedicated module is available.
 *
 * Supported layouts:
 *  - 1 full-width banner  (single card)
 *  - 2 side-by-side banners
 *  - 3 banners in a row (current default)
 */

$ads = [
    [
        'label'  => __('Up to 30% off'),
        'title'  => __('Brake & suspension deals'),
        'text'   => __('Top brands, low prices — limited stock.'),
        'cta'    => __('Shop now'),
        'url'    => '/sale',
        'color'  => 'bg-[var(--color-accent)]',
    ],
    [
        'label'  => __('New in'),
        'title'  => __('LED upgrade lights'),
        'text'   => __('Plug-and-play kits for most popular models.'),
        'cta'    => __('View range'),
        'url'    => '/catalogsearch/result/?q=led',
        'color'  => 'bg-zinc-900 dark:bg-zinc-800',
    ],
    [
        'label'  => __('Free shipping'),
        'title'  => __('Rider gear bundles'),
        'text'   => __('Helmet, gloves & jacket combined. Save more.'),
        'cta'    => __('See bundles'),
        'url'    => '/catalogsearch/result/?q=rider+gear',
        'color'  => 'bg-emerald-700 dark:bg-emerald-800',
    ],
];
?>
<div class="mx-auto w-[92%] sm:w-[90%] md:w-[88%] lg:w-[85%]">
    <div class="grid grid-cols-1 gap-4 sm:grid-cols-3">
        <?php foreach ($ads as $ad): ?>
            <a
                class="group relative flex min-h-[9rem] flex-col justify-end overflow-hidden rounded-xl p-5 text-white no-underline transition hover:opacity-95 hover:shadow-lg <?= /* @noEscape */ $ad['color'] ?>"
                href="<?= $escaper->escapeUrl($block->getUrl('') . ltrim((string) $ad['url'], '/')) ?>"
            >
                <div class="pointer-events-none absolute inset-0 bg-gradient-to-br from-black/0 via-black/10 to-black/40"></div>
                <div class="relative z-10">
                    <span class="mb-1.5 inline-block rounded-md border border-white/40 bg-white/15 px-2.5 py-0.5 text-[0.7rem] font-bold uppercase tracking-widest">
                        <?= $escaper->escapeHtml($ad['label']) ?>
                    </span>
                    <h3 class="text-lg font-extrabold leading-snug tracking-tight">
                        <?= $escaper->escapeHtml($ad['title']) ?>
                    </h3>
                    <p class="mt-1 text-sm text-white/80">
                        <?= $escaper->escapeHtml($ad['text']) ?>
                    </p>
                    <span class="mt-3 inline-flex items-center gap-1.5 text-sm font-semibold underline-offset-2 group-hover:underline">
                        <?= $escaper->escapeHtml($ad['cta']) ?>
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
                    </span>
                </div>
            </a>
        <?php endforeach; ?>
    </div>
</div>
